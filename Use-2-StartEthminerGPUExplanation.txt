ethminer -G //Starts ethminer with a nvidia/amd gpu instead of the cpu --no-precompute //noprecompute means that the dag isnt precomputed. Im not sure what dag is but it decides what block settings the miner should have. I.e. gas limits